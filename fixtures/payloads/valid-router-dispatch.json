{
  "mode": "initial",
  "selected_roles": [
    {
      "role": "generator.implementer",
      "task": {
        "goal": "Add refresh-token handling.",
        "scope": ["src/auth/**", "tests/auth/**"],
        "non_goals": ["Do not change deployment config."]
      },
      "context": {
        "modules": ["auth"],
        "source_paths": ["src/auth/**", "tests/auth/**"],
        "include_artifacts": ["planner_package", "acceptance_contract"],
        "include_feedback": []
      },
      "write_permission": "worktree_write",
      "provider_hint": "mock"
    }
  ],
  "execution_plan": [["generator.implementer"]],
  "aggregation": {
    "strategy": "validate_and_merge",
    "required_checks": ["schema", "scope_check"]
  },
  "rationale": "A single implementer is enough for the fixture task."
}
