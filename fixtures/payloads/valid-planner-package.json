{
  "goal": "Implement refresh-token handling.",
  "success_definition": "The must criterion passes and a final report can be generated.",
  "non_goals": ["Do not deploy."],
  "technical_constraints": ["Keep login API response shape compatible."],
  "known_facts": [
    {
      "fact": "Authentication code lives under src/auth.",
      "source": "repo-context"
    }
  ],
  "inferred_context": [
    {
      "inference": "Tests likely belong under tests/auth.",
      "basis": "Existing test layout.",
      "confidence": "medium"
    }
  ],
  "units": [
    {
      "ref": "auth-refresh",
      "title": "Add token refresh flow",
      "goal": "Implement refresh-token handling without changing login response shape.",
      "scope": {
        "allowed_paths": ["src/auth/**", "tests/auth/**"],
        "forbidden_paths": [".env", "deploy/**"]
      },
      "dependencies": [],
      "recommended_generators": ["generator.implementer"],
      "recommended_evaluators": ["evaluator.contract_checker"],
      "risk_level": "medium",
      "max_fix_rounds": 1
    }
  ],
  "batches": [
    {
      "unit_refs": ["auth-refresh"],
      "parallel": false
    }
  ],
  "contracts": [
    {
      "unit_ref": "auth-refresh",
      "objective": "Refresh token is issued with the access token.",
      "criteria": [
        {
          "ref": "criterion-refresh-issued",
          "description": "Refresh token is issued together with access token.",
          "severity": "must",
          "evidence_required": ["test_result", "diff_reference"],
          "verification": [
            {
              "type": "command",
              "command": "npm test -- auth",
              "expected": "passed"
            }
          ]
        }
      ],
      "forbidden_changes": ["production deployment config"],
      "evaluator_focus": ["backwards compatibility"],
      "auto_fix_hints": [
        {
          "failure_type": "test_failure",
          "role_hint": "generator.implementer"
        }
      ]
    }
  ],
  "routing_hints": {
    "generator": ["generator.implementer"],
    "evaluator": ["evaluator.contract_checker"]
  },
  "risks": []
}
