# Fixtures

`payloads/` contains Milestone 2 LLM payload fixtures used by schema and
artifact-store tests.

Valid payload fixtures must be accepted by the matching `llm.*.schema.json`
schema and can be enriched into canonical artifacts. Invalid fixtures should
exercise stable error classifications.
